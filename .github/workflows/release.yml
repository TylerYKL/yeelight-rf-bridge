name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v1

      - name: Install ESP8266 core
        run: |
          arduino-cli config init
          arduino-cli config add board_manager.additional_urls https://arduino.esp8266.com/stable/package_esp8266com_index.json
          arduino-cli core update-index
          arduino-cli core install esp8266:esp8266

      - name: Install libraries
        run: |
          arduino-cli lib install ArduinoJson

      - name: Prepare sketch folder
        run: |
          mkdir -p firmware
          cp yeelight_rf_bridge_v2.ino firmware/firmware.ino

      - name: Compile
        run: |
          arduino-cli compile --fqbn esp8266:esp8266:d1_mini firmware --output-dir build
          ls -la build/

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/*.bin
          body: "Firmware release ${{ github.ref_name }}"

      - name: Update manifest.json
        run: |
          VER=${GITHUB_REF_NAME}
          BIN=$(ls build/*.bin | head -1 | xargs basename)
          echo "{\"version\":\"${VER#v}\",\"url\":\"https://github.com/${{ github.repository }}/releases/download/${VER}/${BIN}\"}" > manifest.json
          cat manifest.json
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add manifest.json
          git commit -m "Update manifest to ${VER}" || echo "nothing to commit"
          git push
